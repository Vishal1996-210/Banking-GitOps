apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: hsbc-prod

  labels:
    app.kubernetes.io/name: account-service
    app.kubernetes.io/part-of: hsbc

spec:
  type: ClusterIP

  selector:
    app.kubernetes.io/name: account-service

  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
